enum UploadType {
  shopsLogo,
  shopsBack,
  products,
  reviews,
  users,
  chats,
}

enum LayoutType { twoH, three, twoV, one }

enum AuthType {
  login,
  signUpSendCode,
  confirm,
  signUpFull,
  forgetPassword,
  updatePassword,
}

enum DropDownType {
  category,
  subcategory,
  childCategory,
  brand,
  unit,
  country,
  city,
  area,
}

enum SignUpType { phone, email, both }

enum FilterType {
  news,
  popular,
  priceMin,
  priceMax,
}